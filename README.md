# react-native-init shell

copy shell file and open terminal same directory  
sh ./react-native-init.sh    
will run react-native init with typescript and setup prettier, eslint with airbnb code convention 
